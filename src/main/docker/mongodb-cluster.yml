version: '2'
services:
    positutely-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb positutely-mongodb-config
    positutely-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    positutely-mongodb-config:
        image: mongo:3.6.3
        container_name: positutely-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
